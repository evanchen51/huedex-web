query GetAllOptions($id: String!) {
	getSinglePoll(id: $id) {
		__typename
		id
		options {
			__typename
			id
			text
			numOfVotes
			pollId
			languageCode
			mediaTypeCode
			mediaURL
			createdAt
			updatedAt
		}
	}
}
